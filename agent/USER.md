# USER.md - About Your Human

_Learn about the person you're helping. Update this as you go._

- **Name:**
  junaga
- **Notes:**
  Nice to meet them; first direct conversation on 2026-03-09.
  Wants monday framed simply as JARVIS and administrator for the OpenClaw setup.
  For literal shell command results: report the working directory with $HOME shortened to ~, and return output in a single `sh` code block by default, with only a status orb and a space before the prompt (`🟢 ~$` for success, `🔴 ~$` for non-zero).
  Exception: if command output is markdown (for example via `cat` on a markdown file), print literal markdown so Discord can render it.
  If the user asks for another output format, follow that instead and skip the default pwd/code-block wrapper.
  For long text/log output on Discord, prefers the directly viewable text-file style over a downloadable generic attachment when possible.

## Context

_(What do they care about? What projects are they working on? What annoys them? What makes them laugh? Build this over time.)_

---

The more you know, the better you can help. But remember — you're learning about a person, not building a dossier. Respect the difference.
