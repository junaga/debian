#!/bin/sh

cd "$(dirname "$0")"


if test "$(id -un)" != desktop; then
	exec su - root -c "exec login -f desktop --shell $PWD/desktop"
fi

# login --shell overrides $SHELL, so restore Bash for programs that start shells.
export SHELL=/bin/bash

exec start-hyprland
