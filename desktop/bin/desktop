#!/bin/sh

cd "$(dirname "$0")"


if test "$(id -un)" != hypr; then
	exec su - root -c "exec login -f hypr --shell $PWD/desktop"
fi

# login --shell overrides $SHELL, so restore Bash for programs that start shells.
export SHELL=/bin/bash

exec start-hyprland
