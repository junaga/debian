-- Previous design choices that have not yet been migrated.

theme = require("theme")

hl.config({
    general = {
        layout = "scrolling",
        resize_on_border = true
    },
    scrolling = {
        column_width = 1 / 3
    }
})
