# AGENTS.md

## Agent Tools

When the user says they are going to sleep, for example "good night" or "gn", run `bash /usr/local/dev/src/node/log/goodnight.sh`, read the printed `goodnight.log`, and email a summary of that report to the authenticated Gmail account.
